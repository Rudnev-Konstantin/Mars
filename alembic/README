# Alembic Migrations

Этот каталог содержит миграции базы данных, управляемые Alembic.

## Основные команды
- Создать новую миграцию:  
  `alembic revision -m "Описание изменений"`  

- Применить все миграции:  
  `alembic upgrade head`  

- Откатить последнюю миграцию:  
  `alembic downgrade -1`  

## Особенности проекта
⚠ **SQLite ограничения**:  
- Не поддерживает удаление колонок через `ALTER TABLE`.  
- Для сложных изменений используйте `batch_alter_table`.  

🔧 **Ручные правки миграций**:  
- Если Alembic не может автоматически определить изменение (например, переименование колонки), редактируйте файл миграции вручную.  

🚨 **Важно**:  
- Не изменяйте уже применённые миграции (может нарушить целостность БД).  
- Все миграции должны быть закоммичены в Git.  
